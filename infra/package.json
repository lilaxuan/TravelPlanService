{
  "name": "@gonow/infra",
  "version": "0.1.0",
  "main": "dist/app.js",
  "types": "dist/app.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "synth": "cdk synth"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.167.1",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.6.3"
  }
}
